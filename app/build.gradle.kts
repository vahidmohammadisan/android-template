plugins {
    id("template.android.app")
    id("template.android.compose")
    id("template.android.hilt")
}

android {
    namespace = "ir.vahidmohammadisan.template"
    defaultConfig {
        applicationId = "ir.vahidmohammadisan.template"
    }
}

dependencies {


}